<!-- Validation failure messages -->
<% if @bias_rating.errors.any? %>
  <% @bias_rating.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Bias Rating #<%= @bias_rating.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_bias_rating/<%= @bias_rating.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      
      

      <!-- Label and input for news_source_id -->
      <div class="form-group">
        <label for="news_source_id" class="control-label">
          News source
        </label>

        <%= select_tag(:news_source_id, options_from_collection_for_select(NewsSource.all, :id, :category, @bias_rating.news_source_id), :class => "form-control") %>
      </div>

      <!-- Label and input for rating -->
      <div class="form-group">
        <label for="rating" class="control-label">
          Rating
        </label>

        <input type="text" id="rating" name="rating" placeholder="Rating" class="form-control" value="<%= @bias_rating.rating %>">
      </div>

      <button class="btn btn-warning">
        Update Bias Rating
      </button>
      or
      <a href="/bias_ratings/<%= @bias_rating.id %>">Cancel</a>
    </form>
  </div>
</div>
